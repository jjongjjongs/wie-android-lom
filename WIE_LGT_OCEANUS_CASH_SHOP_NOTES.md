# 오셔너스 (LGT `0002D6C4`) — 캐시샵 아이콘 그리드 (보류)

결제는 동작한다. **캐시샵 가운데 아이템 그리드만 안 그려진다** — 그 자리로
게임 월드가 그대로 비친다. 투자 대비 가치가 없다고 판단해 여기서 멈췄다.
아래는 멈춘 지점까지 확정된 것이다.

## 확정: 목록 개수가 0이다

캐시 탭(모드 2)의 슬롯 루프가 개수를 보고 곧바로 빠져나간다.

```asm
3a4ea  adds r3, r2, r1     ; 0x150a5a4 + 0xe15c = 0x1518700
3a4ec  ldr  r3, [r3]       ; 목록 개수
3a4ee  cmp  r4, r3         ; 슬롯 인덱스 vs 개수
3a4f0  bge  0x3a57a        ; 인덱스 >= 개수 -> 이 슬롯 안 그림
```

기기 트레이스에서 **첫 반복(인덱스 0)부터 `3a4f0>3a57a`가 걸린다.**
`0 >= 개수`이므로 `0x1518700`은 0이다. 패널은 정상적으로 열려 있고 드로우
루틴도 정상적으로 돈다 — 그릴 항목이 없을 뿐이다.

## 확정: 그 값을 아무도 쓰지 않는다

`.text` 전체를 훑었다. `0x1518700`을 **읽는 곳 7군데, 쓰는 곳 0군데**다.
베이스가 파라미터로 들어오는 `adds rZ,rX,rY` + `str [rZ]` 패턴까지 포함해
스캔했고, 결과는 전부 읽기(`ldr`)였다.

캡처 5개의 `MC_fsRead`·`MC_knlGetResource` 목적지 중 이 주소를 덮는 것도
없다. 세이브는 `0x1500205`, `0x1507d48`, `0x1508760`(+3464), `0x151e264`(+449)
로 들어온다.

## 다음에 열면 여기서부터

`w:1518700`을 프로브 감시에 걸고 캐시샵에 들어갔다 나오면 끝난다.

- 뭔가 찍히면 → 누가 언제 무슨 값으로 썼는지
- **아무것도 안 찍히면 그것도 답** → 목록을 만드는 코드 경로가 아예 안 돈다

쓰기 감시는 정적 분석이 가장 못하는 질문에 답한다. 값이 세 단계 건너온
포인터로 쓰이면 명령 스트림을 훑어서는 못 찾지만, 쓰기 자체는 놓칠 수 없다.

## 지나간 길 — 반복하지 말 것

여섯 번의 계측 빌드가 들어갔고 그중 넷은 헛수고였다. 왜 그랬는지 남긴다.

- **리소스 잘림 가설: 틀렸다.** 242회 읽기 전부 정상이고 버퍼 크기가 리소스
  크기와 정확히 일치한다. `MC_knlGetResource`의 무언 실패 두 경로를 WARN으로
  드러낸 건 그 김에 한 것이고, 이 건의 원인은 아니다.
- **"패널이 안 열린다": 틀렸다.** 핸들러 `0x35d6c`가 키 `-5`로 열고,
  `0x47ac8`이 0을 돌려주며, 게이트 플래그(`0x1509f08+9`)가 선다.
- **`0x3a364`(드로우 진입)에 감시를 건 것: 실험 설계가 틀렸다.** 이 루틴은 앱
  시작부터 매 프레임 돈다. 감시가 첫 3회에서 기회를 다 쓰는데 그건 캐시샵에
  도달하기 한참 전이고, 그래서 "게이트 닫힘"은 당연한 결과였을 뿐 아무 정보도
  아니었다. 게이트를 **통과한** `0x3a376`에 걸어야 답이 나왔다.
- 화면 썸네일 없이 서페이스 지문(숫자)만으로 어느 영역인지 추측한 것도
  시간을 썼다. 로그와 스크린샷을 같은 순간에 받는 게 먼저다.

## 남은 계측 (전부 범용, 이 타이틀 전용 코드는 없다)

- 수집 구간(수집/종료 버튼) — 전 영역 로그 + 분기 추적
- 오프스크린 서페이스 지문과 ASCII 썸네일, 프레임 썸네일
- 프로브 감시 `pc:`/`w:` — 기기에서 주소 지정
