# 놈ZERO (0002AC84) 인증 프로토콜

LGT WIPI-C `Clet`, Thumb. `.text`는 `0x1000`(파일 오프셋 `0x34`)에 있으므로
`파일오프셋 = va - 0xFCC`.

## 한 줄 요약

응답 프레임을 **요청과 같은 명령 id로 되돌려주고 있었다.** 게임은 그 프레임을
정상적으로 읽고, 성공으로 받아들이고, **아무에게도 알리지 않고 버렸다.**
밖에서 보면 "요청은 답을 받았는데 인증 화면이 움직이지 않는" 모습 그대로다.

## 프레이밍 (`0x44fac`)

수신은 두 단계다.

```
0x44f98  →  4        헤더 길이 상수
0x44f9c  →  [1]<<8 | [0] - 4      본문 길이
```

즉 프레임은 `u16 LE 전체길이` + 2바이트 + 본문. 요청과 같은 모양이다.
본문까지 다 모이면 `0x45024`가 타임아웃 타이머를 끄고(`0x34db8`),
`[[r6]+0xc]`로 디스패처(`0x422b8`)를 부른다.

## 명령 id (`0x422b8`) — 여기가 원인이었다

```asm
422cc  ldrsb r5, [r0, r5]     ; body[0], 부호 있는 상태값
422ce  cmp   r5, #0
422d0  bge   #0x422e8         ; 음수면 [r4][0x10] 에러 콜백
422e8  ldrb  r3, [r0, #3]
422ea  ldrb  r2, [r0, #2]
422ec  lsls  r0, r3, #8
422ee  ldr   r3, [pc, #0x78]  ; 0x103
422f0  orrs  r0, r2           ; id = [3]<<8 | [2]
422f2  cmp   r0, r3
422f4  beq   #0x42310
422f8  bgt   #0x42302         ;   0x107 → 0x42310, 0x201 → 0x42354
422fa  subs  r3, #2           ;   0x101 → 0x42310
4230e  b     #0x42362         ; 그 외에는 조용히 버린다
```

받아들이는 id는 **0x101 / 0x103 / 0x107 / 0x201 넷뿐**이다.

게임이 보내는 요청은 둘이다.

| 보내는 곳 | 요청 id | 답 id |
|---|---|---|
| state 4 `0x424b8` (`r1 = 0x83 << 1`) | `0x0106` 인증 | `0x0107` |
| state 7 `0x4250c` (`r1 = 0x80 << 2`) | `0x0200` 결제 | `0x0201` |

**답의 id는 요청 id + 1.** 요청의 `06 01`을 그대로 돌려주면 `0x106`이 되고,
넷 중 어느 것도 아니므로 `0x4230e → 0x42362`로 떨어진다.
2026-09-10 캡처의 분기 트레이스가 정확히 그 두 줄이다.

```
44f88>4500e 45022>45002 45004>45024 4502a>95050 95050>34db8 ...
95050>422b8 422d0>422e8 422f8>42302 4230e>42362 42366>45050
             ^성공 분기   ^0x106>0x103  ^어디에도 안 맞음 → 버림
```

## 본문 (`0x42310`)

```asm
42310  ldr   r5, [r1, #8]     ; 본문
42316  adds  r1, r5, #1
4231a  bl    #0x41738         ; body[1..0x29] 을 "audio.adt" 에 기록
42322  beq   #0x4234c         ; 기록 실패면 중단
42326  adds  r3, #0x29
42328  ldrb  r3, [r3]         ; body[0x29]
42334  bne   #0x42340
4233c  movs  r3, #0xc         ; 0    → state 12, 구매 확인 창 (1/2 키)
42346  movs  r3, #9           ; 그 외 → state 9,  안내 후 진입
```

본문은 **42바이트**다.

```
[0]        상태. ldrsb 로 읽으므로 음수만 아니면 된다.
[1..0x29]  40바이트. 0x41738 이 그대로 audio.adt 로 흘려보낼 뿐 읽지 않는다.
[0x29]     0이면 구매 확인 창, 0이 아니면 "이미 권한 있음" → 진입.
```

`0x41738`은 본문을 검사하지 않는다. 40바이트를 0x3c 레코드 앞에 복사하고,
레코드 `+0x28`에는 세션의 `+0x70`을, `+0x34`에는 세션의 `+0xe4`를 채운 뒤
`0x21c3` 시드 XOR(`0x4162c`)로 `audio.adt`에 쓰고, **쓰기가 성공했는지**만
돌려준다. 다음 실행의 캐시 검사(`0x416e0`)가 보는 두 칸도 우리가 준 40바이트가
아니라 세션에서 채워지므로, 그 40바이트가 0이어도 잃는 것이 없다.

## 어느 쪽으로 답하는가

`0xbd4c`가 상태머신 틱(`0x42370`)의 반환값을 이렇게 읽는다.

```asm
bd50  bl   #0x95050      ; 0x42370
bd54  cmp  r0, #2
bd56  bne  #0xbd60
bd58  bl   #0xbaa8       ; 2 → 다음 장면으로
bd60  cmp  r0, #0
bd64  bl   #0x48d04      ; 0 → 종료
```

- `body[0x29] != 0` → state 9(`0x4256e`): 안내를 띄우고, 소프트키(-5)나 2초
  경과 후 `[r4+0xf4] = 2`, state 15 → **2를 반환, 진입**.
- `body[0x29] == 0` → state 12(`0x4251c`): 1/2 키를 기다린다. 1이면 state 6을
  거쳐 state 7에서 `0x0200`을 보내고, 그 답(`0x201`)이 state 13으로 옮겨
  같은 자리로 돌아온다. 2면 `[r4+0xf4] = 0` → **0을 반환, 종료**.

그래서 `[0x29]`는 0이 아닌 값으로 답한다. 교환 한 번으로 끝나고, 사용자가
확인 창에서 2를 눌러 게임이 종료될 여지도 없다.

`0x0200`(결제)에 대한 답도 넣어두었다. `0x42354`는 본문을 보지 않고 state 13만
세우므로 상태값 하나면 충분하지만, 답이 없으면 그 자리에 멈춘다.

## 캐시 경로 (`0x416e0`, state 0)

한 번 인증이 끝나면 다음 실행은 네트워크를 타지 않는다.

```
audio.adt 를 읽는다 (0x4165c). 실패 → state 1
memcmp(세션+0x70, 레코드+0x28, 12) == 0 → 0x10 = 이미 인증됨, 틱이 2를 반환
아니면 count = 레코드+0x34;  count == 0 → state 10 (횟수 소진 → 종료)
                             count <= 10 → state 1
                             count >  10 → 0x10
```

`0x41738`이 레코드 `+0x28`에 세션의 `+0x70`을 그대로 써 두므로, 정상 인증
이후에는 memcmp가 맞아 곧바로 진입한다.

## 지나온 길 — 반복하지 말 것

- **요청 명령을 그대로 되돌려주는 것**은 이 프로토콜에서 통하지 않는다.
  다른 LGT 타이틀 다수가 그렇게 동작해서 처음에 그렇게 두었지만, 놈ZERO는
  id로 디스패치하고 모르는 id를 조용히 버린다. 로그에는 아무 흔적도 남지
  않는다 — 프레임은 읽혔고, 오류도 없다.
- **본문을 4바이트로 두면** `body[0x29]`가 프레임 밖이라 디스패처에 닿아도
  쓰레기를 읽는다. 42바이트가 최소다.
- 인증 교환은 실행 후 **10초쯤**에 일어난다. 분기 트레이스에 예산을 두면
  로딩 화면만 담고 끝나 이 교환을 놓친다 (`c24c662` 이전에 두 번 그랬다).
