# 영웅서기5-칠흑의계약 (LGT `00032870`) — 상점 결제 프로토콜

**동작한다.** `billing.rs`의 `lgt_local_hero5_response`가 아래 표의 열두
(command, sub) 단계를 답하고, 그걸로 핸드셰이크·핑·구매·유물함 개봉·창고
입출고까지 간다.

처음 이 문서를 열게 만든 증상은 이거였다. 상점에 들어가면 게임이 멈췄고,
로그가 이유를 그대로 말해줬다:

```
bill write 1: 80 bytes [00 00 00 50 47 31 30 30 30 31 35 37 ...]
LGT billing gateway: a 80 byte request is not one this can shape a reply to
```

`wie_backend::billing::response()`의 프레임 매처 중 이 80바이트 요청의 모양을
아는 것이 없었다. 게임은 응답을 기다리며 그대로 서 있었다.

아래는 그걸 풀려고 `binary.mod`를 읽어서 알아낸 것이다. **표에 없는 명령은
추측으로 답하지 말 것**: 잘못된 답은 타이틀을 멈추게 하는 대신 다른 교환을
위한 분기로 보내고, 그러면 무엇이 틀렸는지조차 안 보인다. 새 (command, sub)가
로그에 뜨면 핸들러를 읽어 필드 수를 세고 표에 한 줄 더 넣으면 된다 — 명령별
핸들러 주소는 아래 점프 테이블 절에 다 있다.

## 바이너리 읽는 법

`binary.mod`는 ELF이고 섹션이 한 파일 안에서 서로 다른 주소로 링크되어 있다. 파일
오프셋 하나로 전체를 훑으면 `.data`가 `.text`인 것처럼 보인다.

| 섹션 | vaddr | 파일 오프셋 |
|------|-------|-------------|
| `.text` | `0x1000` | `0x34` (delta `0xFCC`) |
| `.data` | `0x1400000` | `0x141598` |
| `.bss` | `0x1500000` | — |

코드는 Thumb다. 임포트는 `.data` 안의 16바이트 ARM 트램폴린으로 호출된다:

```
str lr, [sp, #-4]!
bl  resolver          ; resolver가 뒤의 두 워드를 읽는다
.word table           ; 0x1fb
.word func            ; 서비스 번호
```

호출부는 트램폴린 주소를 리터럴 풀에서 읽어 `bx rN` 베니어로 뛴다 — `0x10e218`이
`bx r0`, `0x10e21c`가 `bx r1`, `0x10e220`이 `bx r2`, `0x10e224`가 `bx r3`, 이런 식이다.
그래서 임포트 호출은 `bl <스텁>`으로 찾을 수 없고, 트램폴린 주소를 실은 리터럴 풀을
역참조해야 찾힌다.

| 트램폴린 | 서비스 |
|----------|--------|
| `0x1400a68` | `MC_knlSprintk` (`0x65`) |
| `0x1400bc8` | `MC_knlGetSystemProperty` (`0x7e`) |
| `0x1400eb8` | `MC_netSocketConnect` (`0x25b`) |
| `0x1400ec8` | `MC_netSocketWrite` (`0x25c`) |
| `0x1400ed8` | `MC_netSocketRead` (`0x25d`) |

## 접속

`0x34e08`이 콜백 `0x385b8`을 `0x301c`로 등록한 뒤 `"210.222.18.28"`(va `0x110034`)의
포트 `0x4706` = 18182로 연결한다. `0x32504`가 4를 돌려주는 경우에만 포트 `0x2385` =
9093을 쓴다.

## 프레임

양방향 같은 모양이고 전부 빅엔디안이다. 내부 구조체는 `[u16 길이][u16 커서][와이어…]`
이고 와이어는 구조체 + 4에서 시작한다.

```
[0..4]    u32  전체 길이 (수신 바이트 수와 반드시 일치)
[4..12]   8B   "G1000157"   (수신 시에는 읽지 않는다)
[12..16]  u32  command      (0..8)
[16..20]  u32  sub          (command 1에서는 0..5)
[20..]    필드: u32, 또는 u32 길이 + 그 길이만큼의 블롭
```

직렬화 헬퍼:

| 주소 | 하는 일 |
|------|---------|
| `0x32638` | u32를 빅엔디안 4바이트로 |
| `0x328e4` | u32 필드 추가 |
| `0x32944` | 길이 + 블롭 추가 |
| `0x3323c` | 커서에서 BE u32 읽고 커서 +4 |
| `0x33270` | 길이를 읽고 그만큼 복사, 커서 += 길이 (길이 0이면 아무것도 안 읽는다) |

`0x33f34`가 구조체 +0의 u16 누계 길이를 구조체 +4에 빅엔디안으로 써넣고, `0x33eac`가
구조체 + 4를 그 길이만큼 `0x2b50`으로 보낸다.

## 캡처된 80바이트 요청

`0x34c9c`가 만든다:

```
0x326e8(ctx, buf, 1, 1)                  헤더: 누계 길이 0x14, "G1000157", BE(1), BE(1)
GetSystemProperty("PHONENUMBER") → 0x32944(…, 16)      가입자번호
모델 문자열                      → 0x32944(…, 8)       "Emulator"
0x328e4(ctx[0xd2*4])                                   300
0x328e4(ctx[0xd3*4])                                   101
0x32944(0x150a594, 16)                                 "HERO5.ALL.SS.000"
```

`0x150a594`는 `0x150a570 + 0x24`로, `0xb8d54`가 기동 때 콘텐츠 ID를 채워 넣는 자리다.
누계는 20 + 20 + 12 + 4 + 4 + 20 = **정확히 0x50 = 80**으로, 캡처와 일치한다.

## 응답

`0x385b8`의 이벤트 `0xa`(수신)에서:

- `wire[0..4]`의 BE 길이가 수신 바이트 수와 다르면 `0x24c4`로 리셋하고 버린다.
- 같으면 `0x32668`이 프레임을 20칸 링 버퍼(ctx + 0x80)에 넣는다.

`0x38498`이 큐에서 꺼내:

- 길이가 `0x13`(19) 이하면 버린다 — **응답은 최소 20바이트**.
- 커서를 `0xc`로 놓고(= `wire[12]`) BE u32 두 개를 읽어 `0x38390(ctx, cmd, sub, frame)`.

`0x38390`은 `cmd > 8`이면 무시하고, 점프테이블 `0x111ee8`로 분기한다:

| cmd | 핸들러 |
|-----|--------|
| 0 | `0x33e58` |
| 1 | `0x37cc4` |
| 2 | `0x375b4` |
| 3 | `0x36c50` |
| 4 | `0x36270` |
| 5 | `0x360bc` |
| 6/7/8 | `0x38452` / `0x38464` / `0x38476` |

`0x37cc4`(cmd 1)는 `sub > 5`면 무시하고 테이블 `0x111ea0`으로 분기한다: sub 0은 무시,
1 → `0x37cec`, 2 → `0x37e10`, 3 → `0x37ef6`, 4 → `0x3813c`, 5 → `0x3823e`.

요청이 `wire[12] = 1`, `wire[16] = 1`이었으므로 응답도 cmd 1 / sub 1, 즉 `0x37cec`가
읽는다:

```
[20..24]   u32   결과       → ctx[0xd8*4]   (0이 성공)
[24..]     u32 길이 + 블롭  → ctx[0xd9*4]
[..]       u32   값         → ctx[+0x14] = 값 * 1000
```

성공(`ctx[0xd8*4] == 0`)이고 `0x150a570`의 `+0xc`/`+0x10`/`+0x14`/`+0x18`이 모두 0이면
`0x35728(ctx, 1, 5)`로 다음 요청을 보내고, 아니면 `ctx[0xd1*4]`(0..5)로 테이블
`0x111eb8`을 타고 `0x35728(ctx, 1, 3)` / `(ctx, 1, 2)` 등으로 갈린다.

블롭 길이 0이 허용되므로 이 단계의 최소 응답은 32바이트다:

```
00 00 00 20  "G1000157"  00 00 00 01  00 00 00 01  00 00 00 00  00 00 00 00  00 00 00 00
```

## 상점 카탈로그

가격표는 서버가 아니라 게임 안에 있다 — `res/c/csv/cash_single.dat`(29종),
`cash_network.dat`(34종), `cash_expert.dat`(9종). 세 파일 모두 아래 포맷으로 끝까지
정확히 파싱된다:

```
u16 개수                      (전부 리틀엔디안)
레코드마다: u16 A, u8 id, u16 아이템, u8 이름길이, 이름(EUC-KR),
            u8 x, u8 y, u8 설명길이, 설명(EUC-KR), u16 가격
```

`x`는 `res/c/csv/item_18.dat`의 행 번호이고 `y`는 개수다. 엘릭서(20)은 34행 20개,
작은 유물함은 15행 1개, 달성의부적(5)는 9행 5개 — 세 카탈로그의 모든 행이 그렇게
맞고, 상점에 있는 이름은 전부 `item_18.dat`에만 있다. `item_18.dat`은 레코드마다
`u16 a, u16 b, u16 코드, u8 이름길이, 이름, 4바이트, u8 설명길이, 설명`이고 43행이다.

유물함 4종은 영웅서기4의 보물함과 같은 구조로, 넷 다 같은 아이템(223)에 인덱스만 다르다:

| id | 이름 | 가격 | 아이템 | x |
|----|------|------|--------|---|
| 18 | 작은 유물함 | 1500 | 223 | 15 |
| 19 | 유물함 | 2000 | 223 | 16 |
| 20 | 큰 유물함 | 2500 | 223 | 17 |
| 21 | 오래된 유물함 | 3000 | 223 | 18 |

## 확인된 핸드셰이크

첫 응답(command 1 / sub 1)을 답한 뒤 받은 로그 두 개가 그것을 확인해 주었다. 상점 구매와
창고 접속 둘 다 32바이트 답을 읽고 **다음 요청을 보냈다** — 받아들여진 답은 조용하지 않고
다음 요청을 낳으며, 그 요청이 다음에 읽을 단계를 알려준다.

```
상점: 80B 1/1  →  (답 32B)  →  24B 6/2 [+ u32 4]
창고: 80B 1/1  →  (답 32B)  →  20B 1/3
```

거기서부터 읽어낸 단계들:

| command | sub | 핸들러 | 응답에서 읽는 필드 | 다음 요청 |
|---------|-----|--------|--------------------|-----------|
| 0 | 2 | `0x33e58` | 아무것도 (sub 1만 읽는다) | — |
| 1 | 1 | `0x37cec` | 결과, 메시지, 값 (× 1000으로 저장) | 1/5, 또는 1/3 · 1/2 |
| 1 | 3 | `0x37ef6` | 결과, 메시지, 15바이트를 떼어 쓰는 블롭 | 5/1, 또는 인사 대화상자 |
| 4 | 1 | `0x362a4` | 결과, 메시지 | 4/6 |
| 4 | 6 | `0x365f4` | 결과, 메시지, 행 개수와 그 행들 | 4/7 |
| 4 | 7 | `0x36710` | 결과, 메시지, 화면에 찍는 u32 | — |
| 5 | 1 | `0x360e8` | 결과, 메시지 | 5/2 |
| 5 | 2 | `0x36146` | 결과, 메시지, 저장하는 u32 | — |
| 6 | 2 | `0x35b0a` | 결과, 메시지 | 6/3 |
| 6 | 3 | `0x35b88` | 결과, 메시지, 행 개수와 그 행들 | — |
| 7 | 1 | `0x33b94` | 결과, 메시지, 타이틀이 그리는 본문 | — |

## 0/2 — 킵얼라이브

`0x392b8`이 밀리초 시계를 읽어 `ctx[0xc]/[0x10]`(마지막 요청 시각)에
`ctx[0x14]/2`를 더한 값과 비교하고, 지났으면 `0x35728(ctx, 0, 2, …)`을 부른다.
헤더뿐인 20바이트 프레임이고, `0x34c68`이 `ctx[0xb]`를 1로 세워 두므로 `0x35824`의
대기 UI는 뜨지 않는다.

`ctx[0x14]`는 1/1 응답의 세 번째 필드 × 1000이다. 우리가 0을 보냈으니 간격도 0이
되어 **틱마다** 핑이 나갔다 — 창고 로그의 결제 프레임 38개 중 35개가 이 핑이고, 초당
26개다. 그래서 이제 1/1에 60(초)을 실어 보낸다.

## 7/1 — 창고 인사 다음

1/3의 `ctx[0xd1*4] != 0` 경로(`0x38044`)는 다음 요청을 보내지 않는다. 블롭에서
15바이트를 떼어 `ctx + 0x351`에 넣고, 문자열 `0x98`과 함께 `MC_knlSprintk`로
"…님 반갑습니다."를 만들어 대화상자로 띄우고 끝난다. 이름이 비어 있던 것은 우리가
길이 0짜리 블롭을 보내 `0x33270`이 아무것도 읽지 않았기 때문이다.

그 대화상자에서 OK를 누르면 20바이트짜리 7/1이 나간다. 이 요청의 빌더 `0x340c8`은
`ctx[0xb]`를 세우지 않는 유일한 빌더라서, `0x35728`이 진입할 때 0으로 지운 그대로
`0x35824`가 `0x33744(ctx, 0x155)`로 "Recieve" 진행 대화상자를 띄운다. 답이 오기
전에는 내려가지 않는다 — 네 번째 스크린샷이 이것이다.

`0x33b94`는 sub 1만 처리한다: 결과, 메시지, 그리고 결과가 0이면 0x400 버퍼에 블롭을
하나 더 읽어 `0x33b20`으로 그린다. `0x33b20`은 먼저 `0x32518`로 진행 대화상자를
내린다. 필드 3개, 최소 32바이트.

모든 단계가 **결과**(0만이 에러가 아니다)와 **길이 붙은 메시지**를 먼저 읽고, 그 다음에
그 단계가 원하는 것을 읽는다. 길이 0인 블롭은 아무것도 읽지 않고 커서도 움직이지 않으므로,
각 단계의 최소 응답은 그 단계의 필드 수만큼의 0이다.

행 개수 0은 깨진 목록이 아니라 빈 목록이다 — `0x35c1a`는 무엇을 읽기 전에 현재 행과 개수를
먼저 비교하고, 5/2의 목록(`0x361a0`)도 같다.

## 지금까지 구현한 것

`billing.rs`의 `lgt_local_hero5_response`가 위 표의 여덟 단계를 답한다. 각각 명령을 그대로
되돌리고 나머지는 0이다 — 결과 0, 빈 메시지, 그 단계가 읽는 나머지 필드도 0. 세 자리만
0이 아닌데, 거기서는 0이 "비었다"가 아니라 "틀렸다"이기 때문이다:

- 1/1의 세 번째 필드 = `HERO5_PING_SECONDS`(60). 0이면 핑이 틱마다 나간다.
- 1/3의 마지막 블롭 = 1/1이 실어 온 가입자번호를 15바이트까지. 0이면 인사에 이름이
  빠진다.
- 6/3의 목록 = `HERO5_SHOP_ROWS`에서 구매한 상품 id로 찾은 한 줄, 유물함이면
  `HERO5_BOX_LADDER`에서 뽑은 한 줄. 0이면 산 물건이 가방에 들어오지 않는다.

핸들러를 읽지 않은 명령과 sub는 답하지 않는다. 추측으로 답하면 타이틀이 멈추는 대신 다른
교환을 위한 코드로 들어간다.

## 레퍼런스 — 심볼이 살아있는 안드로이드 이식판

`hero5v1.08vc46.apk`의 `assets/libHeroesLore5.so`는 **스트립되지 않은 32비트 ARM ELF**로,
게임 본체가 심볼째 들어 있다(4329개). 여기서 프로토콜 전체가 이름으로 읽힌다.

| 커맨드 | 핸들러 |
|--------|--------|
| 0 | `NETWORK::ProcPacket_System` |
| 1 | `NETWORK::ProcPacket_Login` |
| 2 | `NETWORK::ProcPacket_Community` |
| 3 | `NETWORK::ProcPacket_Mail` |
| **4** | **`NETWORK::ProcPacket_Inventory`** (창고) |
| 5 | `NETWORK::ProcPacket_ItemTrade` |
| **6** | **`NETWORK::ProcPacket_Shop`** |
| 7 | `NETWORK::ProcPacket_Notice` |
| 8 | `NETWORK::ProcPacket_Event` |
| 100 | `NETWORK::ProcPacket_Ranking` |

읽기 헬퍼도 그대로다: `readNet8/16/32/64`, `readNetBuf`, 그리고 **`readNetItem(int, _NET_ITEM_*)`**가
우리가 `0x333fc`로 부르던 그 함수다. `_NET_ITEM_`은 0x60바이트이고 필드 위치가
`0`, `4`, `5`, `6`(이름), `0x20`, `0x28`, `0x30`, `0x38`, `0x3a`, `0x3c`, `0x3d`, `0x3e`,
`0x40`, `0x42`, `0x44`, `0x45`… 로 우리가 읽어낸 순서와 정확히 같다.

### 4/6의 정확한 모양

`ProcPacket_Inventory` sub 6:

```
readNet32                  → 결과
readNetBuf                 → 메시지
결과 != 0 이면 팝업
NetStorageItem::DeleteAll()
readNet32                  → n
NetStorageItem::SetSize(n)
n번 반복:
    readNet32              → 칸 번호
    readNet8               → 상태
    memset(item, 0, 0x60)
    readNetItem(&item)
    NetStorageItem::NewNetStorageItem(&item, 상태, 칸 번호)
WriteData(4, 7)
```

`NewNetStorageItem`은 아이템을 `this + 칸*4 + 8`, 개수를 `this + 칸 + 0x148`,
상태를 `this + 칸 + 0x198`에 넣는다 — WIPI판 `0x1507c`와 같은 배치다. 개수가 0 이하면
세 자리를 전부 0으로 쓴다.

### 4/3 — 창고에서 인벤으로

`PacketWrite_Inventory`의 세 번째 갈래가 요청을 만든다:

```
writeNet32(커서 + 페이지 * 16)     ← 창고 칸 번호
writeNet32(개수)
```

캡처의 `[0][1]`이 그것이다 — 0번 칸에서 1개.

`ProcPacket_Inventory` sub 3이 응답을 읽는다:

```
readNet32   → 결과
readNetBuf  → 메시지
결과 != 0 이면 팝업
memset(item, 0, 0x60); readNetItem(&item)
BagItem::NewBagNetItem(&item, item.개수, 1)
HERO::SaveBagData()
WriteData(4, 6)
```

즉 **응답이 실어 보내는 레코드가 곧 가방에 들어가는 물건**이고, 그 레코드 안의 개수가
실제로 들어가는 수량이다. 지금은 요청한 칸의 레코드를 그대로 돌려주고 창고에서 뺀다.
요청한 수량이 든 수량보다 적으면 나머지는 창고에 남긴다.

### 등급과 옵션

`writeNetItem(int, ItemInfo*, int)`이 와이어 ↔ 아이템 필드를 그대로 말해 준다. 레퍼런스
빌드의 `ItemInfo`는 WIPI판보다 4바이트 뒤에 있을 뿐 순서가 같다:

| 와이어 필드 | 레퍼런스 | WIPI | 뜻 |
|-------------|----------|------|-----|
| 15 | `+0x15c` | `+0x158` | **등급** (`ItemInfo::GetGradeColorTag`이 읽는 그 바이트) |
| 16 | `+0x15d` | `+0x159` | 제한레벨 (`EquipItemInfo::GetLevelLimit`) |
| 17 | `+0x15e` | `+0x15a` | — |
| 18 / 19 | `+0x15f` / `+0x162` | | 옵션0 id / 값 |
| 20 / 21 | `+0x160` / `+0x163` | | 옵션1 id / 값 |
| 22 / 23 | `+0x161` / `+0x164` | | 옵션2 id / 값 |

**중간 네 개가 뒤바뀐 것처럼 보였던 이유가 이거다** — 아이템 안에서는 id 셋과 값 셋이
따로 모여 있고, 와이어는 그걸 (id, 값) 쌍으로 엮어 보낸다.

`ItemTable::MakeItemOption(item)`은 등급으로 옵션 **개수**를 정하고
(1→1개, 2→2개, 3 또는 5 이상→3개, 0·4→없음), `SetItemOption`이 `option.dat`
(4바이트 헤더 + 10바이트 레코드 24개: `u8 id, s8 계수, u16 누적확률 ×3`)에서 굴린다 —
값은 `계수 × 제한레벨 ÷ 100 × Rand(80,120)%`를 1~100으로 자른다. 드롭템은 그렇게
굴리지만, **우리는 굴릴 필요가 없다**: 아이템 테이블 행이 이미 자기 등급과 옵션을 들고
있어서 행을 그대로 보내면 타이틀이 드롭한 것과 같은 물건이 된다.

행의 마지막 아홉 바이트(등급, 제한레벨, 하나 더, 옵션 세 쌍)는 **순서를 바꾸지 않고
그대로** 보낸다. `0x32dfc`에서 가운데 네 개가 뒤바뀐 것처럼 보였던 이유가 바로 그 옵션
쌍이다 — 아이템 안에서는 id 셋과 값 셋이 따로 모여 있고 와이어가 (id, 값)으로 다시
엮는데, 그게 곧 테이블 행이 이미 가진 순서다.

### 상태 바이트 = 거래 상태

`NetStorageItem::GetItemState(short)` = `this[칸 + 0x198]`이고,
`GetItemState(char page, short i)` = `GetItemState(page*16 + i)` — **창고는 한 페이지 16칸**이다.

그리고 `StateNetMenu::KeyStorage`가:

```
state = GetItemState(page, cursor)
if state == 1: GetMenuText(0xaa) 를 띄우고 끝
```

`menu_text.dat` 170번이 **"거래중인 아이템입니다."** 다. 우리가 1을 보내서 넣은 아이템이
전부 판매중으로 잠긴 것이다. **보관 상태는 0**이다.

## 4/1, 4/6, 4/7 — 창고관리

창고 메뉴에서 창고관리를 누르면 4/6이 나가고, 답이 없으면 "Recieve"에서 멈춘다.
`0x365f4`는 결과와 메시지를 읽은 뒤 `0x15a54`로 목록을 비우고, u32 개수를 읽어
`0x1502c`로 크기를 잡고, 행마다 `u32`, `u8`, 그리고 6/3과 **같은 88바이트 레코드**
(`0x333fc`)를 읽어 `0x1507c`로 목록에 넣는다. 즉 창고 칸도 인벤 아이템과 같은 모양이다.
목록을 다 읽으면 `0x35728(ctx, 4, 7)`을 보낸다.

`0x36710`(4/7)은 결과와 메시지 다음에 u32 하나를 읽어 화면 객체의 첫 워드에 넣는다 —
창고 화면 맨 위의 "거래용 화폐"다. 다음 요청은 없다.

4/7은 화폐 0으로 답한다.

### 4/1 — 창고에 넣기

창고에 아이템을 올리면 43바이트짜리 4/1이 나간다. 캡처:

```
00 00 00 2b "G1000157" 00 00 00 04 00 00 00 01
00 00 00 01  0d  26  00 00 00 09  be e3 c0 ba 20 b0 a1 c1 d7  00 00 00 00
             ^개수 1  ^테이블 13  ^38행   ^길이 9   ^"얇은 가죽"        ^ctx[0x564]
```

`0x3435c`의 sub 1이 만든다: 가방에서 `(ctx[0x7c0], ctx[0x7c1])` 칸의 아이템을
`0x114ac`으로 꺼내 `0x32dfc`로 레코드 하나를 쓰고, 그 뒤에 `ctx[0x564]` u32 하나를
붙인다. 즉 **4/1의 몸통 = `0x333fc`가 읽는 그 레코드 + u32 하나**다.

응답 핸들러 `0x362a4`는 결과와 메시지 **둘만** 읽는다. 결과가 0이면 가방
(`0x150a570 + 0x13a0`)에서 그 칸을 지우고 `0x35728(ctx, 4, 6)`으로 목록을 다시 받는다.
아이템이 그 순간 가방에서 빠지므로 **넣은 것을 우리가 기억하지 않으면 아이템이
사라진다** — 그래서 `HERO5_WAREHOUSE`가 레코드를 그대로 들고 있다가 4/6 목록에
`[u32 칸][u8 0][레코드]`로 돌려준다. 영웅서기4의 `HERO4_WAREHOUSE`와 같은 자리이고,
저장소 키는 `hero5_warehouse`다.

레코드 길이는 선언을 따라 잰다: 개수 u32(0이면 거기서 끝), 테이블 u8, 행 u8,
길이+이름, 그리고 **테이블이 10 이하면 58바이트 더**. 못 재는 프레임은 반쪽 레코드를
넣느니 아무것도 안 넣는다 — 목록은 이걸 그대로 되돌려주므로 반쪽이 들어가면 그 뒤 행이
전부 밀린다.

### 4/6 목록 행의 상태 바이트

`0x1507c`는 리스트에 **평행 배열 세 개**를 쓴다: 아이템 포인터가
`list + 칸*4 + 8`, 개수가 `list + 칸 + 0x148`, 그리고 행이 실어 온 바이트가
`list + 칸 + 0x198`이다. 개수가 0 이하인 행으로 들어오면(`0x156ec`) 세 자리를 전부
0으로 쓴다 — 즉 **`+0x198`의 0은 "빈 칸"** 이다.

처음엔 그 바이트를 0으로 보냈고, 그러자 넣은 아이템이 창고에 보이지 않았다. 로그상
프레임은 받아들여졌다 — `MC_knlCalloc(0x134)`가 행 수만큼 찍혀 아이템 객체가 실제로
만들어졌고, 4/7로 보낸 거래용 화폐 0은 **같은 구조체의 첫 워드**라 화면에 그대로 나온다
— 그런데 격자에는 없었다. 그 바이트를 1로 바꿔도 마찬가지였다.

모든 칸을 열거하도록 바꾸자 아이템이 격자에 나타났다. 다만 레퍼런스가 말하는 원래 모양은
`SetSize(n)` 뒤에 **n번 반복**이므로 개수는 "몇 칸인가"가 아니라 **행 수**다 — 우리가
20칸을 열거하는 것은 빈 행 19개를 함께 보내는 것이라 그 정의와도 맞는다.

그리고 나타난 아이템은 "판매중"으로 잠겨 있었다. 상태 바이트를 1로 보냈기 때문이고,
1은 거래중이다(위 레퍼런스 절). 지금은 0을 보낸다.

아직 못 본 것: 창고에서 **꺼내는** 요청. 4/2~4/5 중 하나일 텐데 핸들러를 안 읽었다.

## 6/3 — 구매 물품 전달

상점에서 엘릭서를 사면 "구매되었다"는 표시는 떴는데 인벤에는 들어오지 않았다. 6/3
응답의 행 개수를 0으로 보냈기 때문이다. 이 목록은 영수증이 아니라 **물건 그 자체**다.

행 하나는 `0x333fc`가 88바이트 구조체로 읽는다:

| 순서 | 읽는 것 | 들어가는 자리 |
|------|---------|---------------|
| 1 | u32 개수 (`0x3323c`) | `rec[0]` — 0 이하면 여기서 끝 |
| 2 | u8 (`0x333d8`) | `rec[4]` — 아이템 테이블 |
| 3 | u8 (`0x333d8`) | `rec[5]` — 그 테이블의 행 |
| 4 | 길이 + 블롭 (`0x33270`) | `rec + 6` — 이름 |
| 5 | `rec[4] <= 10`일 때만 | 장비 레코드 (u64 네 개, u16 여럿, u8 여럿) |

`rec[4]`가 테이블인 것은 `0xe748`이 말해 준다: 첫 인자를 `0x12`와 비교하고 19칸
점프테이블 `0x110af4`을 타는데, `item_00.dat`…`item_18.dat`과 정확히 같은 수다.
두 번째 인자는 `mgr[테이블*4 + 0x28] + 행*0x170 + 0x18`으로 이름을 꺼낸다. 상점 물건은
전부 `item_18.dat`에 있으므로 테이블은 항상 **18**이고, 행은 `cash_*.dat`의 `x`다.

`0x35c1a`의 목록 루프는 행마다 `0x333fc`로 읽고, 장비면 `0xdf88`로 장비 구조체를
만들고, 아니면 `0x35ea4`가 가방(`0x150a570 + 0x13a0`)에 `0x109a8(가방, rec, 개수, 1)`로
그대로 넣는다. 이름은 `0xe748`로 다시 꺼내 쓰므로 응답의 이름 블롭은 비워도 된다.

요청 `6/3`의 첫 필드가 구매한 상품 id다 — 첫 캡처의 `[+u32 4]`가 엘릭서(20),
두 번째 캡처의 `[+u32 0x13]`이 유물함이다. 카탈로그에 없는 상품 id는 빈 목록으로 답한다.

## 유물함 — 영웅서기4와 같은 방식인가

**아니다.** 두 타이틀 다 서버가 상품을 정하지만, 정하는 방법이 다르다.

| | 영웅서기4 | 영웅서기5 |
|---|---|---|
| 지급 메시지 | `5/0x40` | `6/3` |
| 담는 것 | 상태 + **바이트 하나** | 아이템 행 목록 |
| 그 바이트 | `0xff`면 "상점 행의 물건 그대로", 아니면 `/ITM/DAT/_ITM_CASH_RANOMBOX`의 인덱스 | — (그런 필드가 없다) |
| 뽑기 테이블 | 아카이브 안에 있다 | **없다** |

영웅서기5의 6/3 행은 `(아이템 테이블, 그 테이블의 행, 개수)`가 전부다. "상점 행의
물건 그대로"에 해당하는 값이 없고, 랜덤박스 테이블도 아카이브에 없다 —
`rewards_*.dat`는 퀘스트 보상, `probability.dat`는 제련 확률표다. 그러니 통신사 서버가
**뽑은 물건 자체를 행으로 보냈다**. 우리가 상자 행을 그대로 돌려주니 상자가 가방에
들어간 것이다.

타이틀이 상자를 여는 것도 아니다. `item_18.dat` 15~18행에는 각자의 아이템 클래스가
있지만(`0x114468`의 15 → `0x79e78`, 16·17 → `0x79e9c`, 18 → `0x79ec0`), 세 클래스의
vtable(`0x110890` / `0x1108a8` / `0x1108c0`)은 항목이 완전히 같고 일반 메서드 세 개뿐이다.
생성자가 다른 것은 이름 문자열 id뿐이다.

그래서 **뽑기는 우리가 해야 한다** — 영웅서기4의 `HERO4_BOX_DRAWS`가 이미 그런 것처럼.
### 등급 — `item_NN.dat` 한 행의 13번째 바이트

이게 모든 걸 푸는 열쇠였다. 767개 장비 행의 그 바이트 분포:

등급 이름은 `menu_text.dat` 87~92에 있고(노멀·레어·에픽·영웅·전설·세트),
`common_text.dat` 215~219에 **이름 앞에 색 태그가 붙은 형태**로 한 번 더 있다:
`&레어|`, `}에픽|`, `` `영웅| ``, `^전설|`, `<세트|`. 그래서 등급 번호는
**0 노멀, 1 레어, 2 에픽, 3 영웅, 4 전설, 5 이상 세트**다.

| 값 | 등급 | 개수 | 비고 |
|----|------|------|------|
| 0 | 노멀 | 362 | 상점에서 파는 것 |
| 1 | 레어 | **0** | 테이블에 하나도 없다 |
| 2 | 에픽 | 6 | 전부 장신구 |
| **3** | **영웅** | **252** | 스톰브링거, 팬텀아머 같은 고유 이름 |
| **4** | **전설** | **81** | 그람:궁극의힘, 라그나블로커처럼 칭호 붙은 것 |
| **5~18** | 세트 | 4씩 | `set_option.dat`의 14개 세트, 각 4부위 |

세트 id는 `set_option.dat`의 행 0~13에 5~18이 그대로 대응한다:

| id | 세트 | id | 세트 | id | 세트 |
|----|------|----|------|----|------|
| 5 | 가시나무 | 10 | 드래곤본 | 15 | 아체야키 |
| 6 | 드레이크 | 11 | 와이어트 | 16 | 푸른비룡 |
| 7 | 나이트메어 | 12 | 로크 | 17 | 폭군쟈칼 |
| 8 | 피닉스 | 13 | 워헤드 | 18 | 지크문드 |
| 9 | 엔젤윙 | 14 | 드래곤 | | |

그리고 **각 행의 마지막 6바이트가 그 아이템 자신의 옵션** 세 쌍 `(옵션 id, 값)`이다 —
팬텀의부적은 (17,5) (21,2) (18,5), 가시나무투구는 (32,5) (31,6) (없음). 일반 등급 행은
전부 `ff 00 ff 00 ff 00`, 즉 옵션 없음. **첫 판이 껍데기였던 이유가 이거다** — 등급 0짜리
행을 골라 보냈으니 흰 이름에 옵션 없는 물건이 나온 것이다.

### 상자별 지급 테이블

| 상품 | 상자 | 가격 | 뽑는 것 | 개수 |
|------|------|------|---------|------|
| 18 | 작은 유물함 | 1500 | **장신구** (`item_10.dat`, 에픽~전설) | 16 |
| 19 | 유물함 | 2000 | **영웅** (등급 3) | 252 |
| 20 | 큰 유물함 | 2500 | **전설** (등급 4) | 81 |
| 21 | 오래된 유물함 | 3000 | **세트** 4부위 통째로 | 13세트 |

신비조사회링(레벨 99, 가격 0)만 장신구에서 뺐다.

### 장비 레코드의 58바이트 꼬리

`0x333fc`는 테이블이 10 이하(= `item_00.dat`…`item_10.dat`, 장비)면 필드를 30개 더
읽는다: u64 세 개, u16 두 개, u8 두 개, u16 세 개, u8 열여덟 개, u16 두 개 — 와이어로
58바이트다. 처음엔 이 58바이트를 전부 0(일곱 개만 `0xff`)으로 보냈고, 그 결과가 **껍데기 장비**였다
— 이름과 아이콘은 맞는데 제한레벨 0, 물리방어 0, 마법방어 0. 꼬리가 곧 능력치이기
때문이다.

꼬리의 정체는 **아이템 객체 `+0x134`~`+0x16e`**이고, 요청을 만드는 `0x32dfc`가
그 순서를 그대로 말해 준다:

| 와이어 | 크기 | 아이템 객체 |
|--------|------|-------------|
| 1~3 | u64 ×3 | `+0x134`, `+0x13c`, `+0x144` |
| 4~5 | u16 ×2 | `+0x14c`, `+0x14e` |
| 6~7 | u8 ×2 | `+0x150`, `+0x151` |
| 8~10 | u16 ×3 | `+0x152`, `+0x154`, `+0x156` |
| 11~19 | u8 ×9 | `+0x158`, `+0x159`, `+0x15a`, `+0x15b`, **`+0x15e`, `+0x15c`, `+0x15f`, `+0x15d`**, `+0x160` |
| 20~28 | u8 ×9 | `+0x161` … `+0x169` |
| 29~30 | u16 ×2 | `+0x16a`, `+0x16c` |

가운데 네 개가 순서가 뒤바뀌어 있다.

그리고 **`+0x14c`~`+0x160`이 `item_NN.dat` 한 행의 21바이트와 정확히 일대일**이다.
드루이안워커(`item_08.dat` 50행)의 21바이트는 `+0x152`=43, `+0x154`=43, `+0x159`=66을
주는데, 이게 타이틀이 찍는 물리방어 43 · 마법방어 43 · 제한레벨 66이다. 그래서
`HERO5_BOX_SETS`는 각 행의 21바이트를 그대로 들고 있고 `hero5_equipment_tail`이 위
순서로 재배열해 보낸다.

나머지는:
- u64 세 개는 0. `0x343ee`가 `+0x134`/`+0x13c`가 0일 때 시계값을 찍어 넣으므로 0이
  "아직 안 찍힘"이다.
- `+0x161`~`+0x164`는 0, `+0x165`~`+0x169`는 `0xff`. `0xdf88`이 테이블 행을 복사한 뒤
  `+0x164`를 0으로 하고 옵션 슬롯 다섯 개를 -1로 채우는 것과 같다.

## 다음 단계

로그의 `bill write`가 계속 검증이 된다. 표에 없는 (command, sub)가 새로 나오면 그
핸들러를 읽어 필드 수를 세고 표에 한 줄 더 넣으면 된다. 명령별 핸들러 주소는 위의 점프
테이블 절에 다 있다.

하네스로 재현하는 길은 아직 막혀 있다. 상점은 게임 안이라 세이브를 불러 한참 진행해야
하고, `capture_scripted_archive`의 키 스크립트는 인트로 애니메이션 길이에 따라 눌린
자리가 밀린다(세 번 시도해 모두 메뉴가 아니라 슬롯선택으로 들어갔다). 결제 세션을 여는
`0x38874`는 UI 코드 열 군데에서 함수 포인터로 불리므로, 그중 어느 것이 메인메뉴에서
닿는지 짚으면 하네스 안에서도 반복할 수 있다.
