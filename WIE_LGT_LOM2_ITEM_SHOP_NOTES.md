# 레전드오브마스터2 (000308FF) — 아이템샵 화면

`binary.mod`는 ARM Thumb, 로드 기준 `VA = off - 0x34 + 0x1000`.

## 화면은 게임이 스스로 그린다

화면 번호는 `0x1501dac`, 분기표 기준 주소는 `0x5678c`, 상한은 `0x3e`
(`0x300e4`–`0x300f6`):

```
r3 = [0x1501dac]
if (r3 > 0x3e) -> 0x31870      ; 아무것도 안 함
pc = [0x5678c + r3*4]
```

표에서 두 항목이 아이템샵에 해당한다.

| 번호 | 주소 | 역할 |
|---|---|---|
| 8 | `0x314a0` | 통신 틱 — 요청을 보내고 응답을 기다린다 |
| 14 | `0x315a8` | **아이템샵 그리기** (`0x28a38` → `0x29368`) |

`0x29368`이 `[아이템샵]`(`0x5532c`) 제목과 목록을 그린다. 목록 개수는
메뉴 자원의 4번 항목에서 읽고, 항목 하나는 20바이트짜리 기술자다. 즉
**화면과 목록은 게임이 가진 것으로 스스로 그린다.** 우리가 끼워 넣을
것은 없다.

## 8번 화면의 통신 상태기계

`0x15050f0`이 통신 상태이고, 8번 화면은 그 값만 본다 (`0x314a8`):

| 값 | 뜻 | 8번 화면의 동작 |
|---|---|---|
| 2 | 연결됨/유휴 | 요청을 만들어 보낸다 (`0x3a364`) |
| 3 | 전송 중 | **아무것도 안 함 — 그냥 기다린다** |
| 4 | 응답 도착 | 수신부(`0x24da4`)로 넘긴다 |
| 5 | 오류 | 화면을 7로 돌린다 |

대기 오버레이 `DATA전송중`(`0x5329c`)은 `0xee1c`에서 그려진다.

수신부는 응답을 처리한 뒤 상태를 **다시 3으로** 돌려놓는다
(`0x24f1c`–`0x24f20`). 즉 한 번의 응답으로는 끝나지 않고, 다음 것을
계속 기다리는 구조다.

## 실측 (2026-09-11 로그)

```
bill write 1: 45 bytes [00 2d 03 e8 ...]    <- 진입 시 한 번 (명령 1000)
bill write 1: 6 bytes  [00 06 27 10 00 00]  <- 이후 40ms마다 무한 반복
```

`MC_knlSprintk` 호출은 `DATA전송중` 215회, 그 외에는 `%s` 2회뿐이다.
**`엉뚱한 패킷날라옴`(`0x54a38`)과 `인덱스:%d`(`0x54a4c`)는 한 번도
찍히지 않았다.** 명령 10000으로 답한 응답은 거부당하지 않고 정상
처리되고 있다는 뜻이다. 남은 문제는 내용이 아니라 **순서**다.

## 모드 바이트

`+0x835`가 수신 분기표를 고른다 (`0x24f3c`):

- 2 또는 3 → 1001/1003/1011/1013/1021/1023/1025/1200
- 그 외 → 3702·3802·3806·3810·3814·4001-4004·10000·30000-30004 (외 다수)

이 값은 요청 패킷에도 실려 나간다 (`0x3a3fe`, 본문 25번째 바이트).
실측된 45바이트 요청에서는 **0**이므로 큰 쪽 표가 적용된다.

30000–30004는 전부 통신사 거절 문구 전용 핸들러다:

```
30000 -> 0x27fa0  일일 사용금액 초과입니다.
30001 -> 0x27fe6  고객님은 월 구매한도로 인해 ...
30002 -> 0x28038  총 사용금액 초과입니다.
30003 -> 0x28074  LGT임직원은 사용이 불가함을 ...
30004 -> 0x280c6  펌웨어 업그레이드 이후 사용하세요.
```

## 다른 타이틀과 다른 점

영웅서기5·오셔너스·서든어택포켓은 이 소켓을 **통신사 결제 게이트웨이**
용도로만 썼다. 한 번 묻고 한 번 답하면 끝이라 대역 응답 하나로 충분했다.

레전드오브마스터2는 같은 소켓을 **자기 게임 서버 채널**로 쓴다. 분기표가
둘, 명령이 수십 개, 진행률 표시(`DATA전송중(%d/%d)`, `+0x828`/`+0x82a`)까지
있는 다단계 프로토콜이다. 그래서 한 번 답하고 끝나지 않는다.

## 응답은 요청 + 2

분기표(`0x25758`)의 명령은 전부 `02` 또는 `04`로 끝난다. 요청이 `00`으로
끝나므로 응답은 **+1이 아니라 +2**다. 1001은 표에 아예 없고, 그래서
`엉뚱한 패킷날라옴 / 인덱스:1001`이 나왔다.

1002는 표의 세 번째 비교에 있다:

```
0x2579a  subs r3, #0x64      ; 0x44e - 0x64 = 0x3ea = 1002
0x2579c  cmp  r2, r3
0x2579e  bne  0x257a2
0x257a0  b    0x25b10
```

`0x25b10`이 곧 `0x1501dac`에 14를 쓰는 블록이다. 오류 경로의 연장이
아니라 **1002번 핸들러**이며, 바이너리에서 14를 쓰는 유일한 곳이다.

진입 조건은 `r1 == 0`인데, 호출부(`0x24f3e`)가 r1에 `+0x835`의 모드
바이트를 담아 넘긴다. 상점 요청은 본문 26번째 바이트에 그 모드를 0으로
싣고 나가므로 조건이 성립한다.

그 다음은 `[0x15050f4+8]`(= 응답 본문의 첫 바이트)로 갈린다. 0이면
1200을 인자로 한 호출 뒤 돌아가고, 0이 아니면 `0x25b38`에서 추가
설정을 한다. 어느 쪽이든 화면 14는 이미 쓰인 뒤다.

## 적용 범위

레전드오브마스터2의 상점 요청만 옮긴다 — 명령 1000에 45바이트 레코드.
영웅서기5는 55바이트 레코드를 쓰고 자기 응답을 요청 + 1로 읽으므로
그대로 둔다.


## 상점 목록은 응답이 싣고 온다 (게임 파일에 없음)

1200의 답 1202를 받는 핸들러가 `0x27250`이고, 목록을 이렇게 만든다:

```
0x2726a  memcpy(0x1505944, 본문+0, 16)   ; 16바이트
0x27270  ldrb r0, [본문+16]              ; 품목 개수
0x27276  lsls r0, r0, #2                 ; 개수 × 4
0x27278  bl   (alloc)
0x27282  str  r0, [r4, #0x14]            ; 행 포인터 배열
0x27286  cmp  r3, #0 ; ble 0x272ca       ; 0이면 목록 없음
```

그 다음 루프(`0x2728a`–`0x272c8`)는 `r7`을 0x15에서 시작해 한 바퀴마다
**0x64(100)씩** 늘리며 응답 버퍼에서 100바이트씩 복사한다. 즉

- **본문 16번째 바이트** = 품목 개수
- **본문 17번째 바이트부터, 한 품목당 100바이트**

게임이 들고 있는 `iItem.lzm` / `itemName.lzm` / `inv.lzm`은 (GLPK 컨테이너
+ LZMA, `0x1c`가 속성 바이트) 아이템 *정의*와 이름 폭 테이블이다. 인벤토리와
마을 상점이 쓰는 것이고, **아이템샵이 무엇을 파는지는 들어 있지 않다.**
그건 서버가 매번 내려주던 상태다.

그래서 지금 40바이트 응답으로는 행이 하나도 안 들어가고, `<- 1 / 0 ->`
(`0x524e4`)은 그 사실을 그대로 보여주는 것이다.

이름도 같다. `%s님 반갑습니다.^^`(`0x553a4`)의 `%s`는 `0x15050f4+0x850`을
가리키고, 거기에 이름을 채우는 것은 1002 핸들러의 `0x25b38` 갈래인데,
그 갈래는 **응답 본문 첫 바이트가 0이 아닐 때만** 탄다. 0일 때는
`0x25b26`으로 가서 1200을 묻고 돌아간다 — 지금 우리가 타는 길이다.
